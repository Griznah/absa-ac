{
  "name": "ac-bot-frontend-tests",
  "version": "1.0.0",
  "description": "Integration tests for AC Bot frontend",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
